# CMC_Cryptocurrencies_Mini_Project
A mini data engineering project that pulls live cryptocurrency data (USD, IDR, EUR) from the CoinMarketCap API, wraps the fetching logic in a Python class, cleans and standardizes the data (including UTC → WIB datetime conversion), and exports the final dataset to CSV.
